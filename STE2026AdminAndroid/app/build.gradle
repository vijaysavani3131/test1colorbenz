plugins {
    id 'com.android.application'
}

android {
    namespace 'com.isavgo.ste2026admin'
    compileSdk 35

    defaultConfig {
        applicationId 'com.isavgo.ste2026admin'
        minSdk 23
        targetSdk 35
        versionCode 1
        versionName '1.0.0'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
