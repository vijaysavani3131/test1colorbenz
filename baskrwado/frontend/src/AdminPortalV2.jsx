import React, { useEffect, useMemo, useRef, useState } from 'react';
import { api } from './lib/api';

const STATUS_OPTIONS=['intake','needs_info','ready_for_review','in_progress','waiting_customer','waiting_external','resolved','closed'];
const PRIORITIES=['low','normal','high','urgent'];
const ROLES=['owner','admin','agent','reviewer'];
const money=(p=0)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format((p||0)/100);
const when=v=>v?new Date(v).toLocaleString('en-IN',{dateStyle:'medium',timeStyle:'short'}):'—';
const title=(v='')=>v.replaceAll('_',' ').replace(/\b\w/g,m=>m.toUpperCase());
const manager=u=>['owner','admin'].includes(u?.role);
const panel={background:'#fff',border:'1px solid #e6e9e5',borderRadius:18,padding:20,boxShadow:'0 10px 30px rgba(16,24,40,.04)'};
const input={width:'100%',border:'1px solid #d7ddd5',borderRadius:10,padding:'11px 12px',font:'inherit',background:'#fff'};
const grid={display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(210px,1fr))',gap:14};

function Login({onLoggedIn}){
 const [form,setForm]=useState({email:'',password:''}),[busy,setBusy]=useState(false),[error,setError]=useState('');
 const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{const r=await api.adminLogin(form.email,form.password);localStorage.setItem('bkw_admin_token',r.token);onLoggedIn(r.user)}catch(err){setError(err.message)}finally{setBusy(false)}};
 return <div className="admin-login-page"><div className="admin-login-card"><div className="admin-login-brand"><span className="brand-mark">B</span><div><strong>BasKarwaDo</strong><small>Operations Console</small></div></div><div className="admin-login-copy"><span className="section-eyebrow">SECURE STAFF ACCESS</span><h1>Resolve cases, not spreadsheets.</h1><p>Role-scoped case operations with private evidence and audit trails.</p></div><form onSubmit={submit} className="admin-login-form"><label>Email<input type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Password<input type="password" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<div className="error-box">{error}</div>}<button className="btn btn-dark admin-login-button" disabled={busy}>{busy?'Signing in…':'Sign in securely →'}</button></form></div></div>;
}

function playNotificationSound(){
 try{const Ctx=window.AudioContext||window.webkitAudioContext;if(!Ctx)return;const ctx=new Ctx();const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type='sine';osc.frequency.setValueAtTime(880,ctx.currentTime);gain.gain.setValueAtTime(.0001,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.13,ctx.currentTime+.015);gain.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+.22);osc.connect(gain);gain.connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+.23);osc.onended=()=>ctx.close();}catch{}
}

function NotificationDropdown({open,onClose,notifications,setNotifications,user,onUser}){
 const prefs=user.notification_preferences||{};const [draft,setDraft]=useState({...prefs}),[busy,setBusy]=useState(false),[error,setError]=useState('');
 useEffect(()=>setDraft({...prefs}),[user.id,JSON.stringify(prefs)]);
 if(!open)return null;
 const labels={new_case:'New case',assigned_case:'Assigned to me',urgent_case:'Urgent case',payment_received:'Payment',customer_update:'Customer update'};
 const save=async()=>{setBusy(true);setError('');try{const r=await api.adminUpdateNotificationPreferences(draft);onUser({...user,notification_preferences:r.preferences})}catch(e){setError(e.message)}finally{setBusy(false)}};
 const mark=async n=>{if(n.read_at)return;await api.adminReadNotification(n.id);setNotifications(items=>items.map(x=>x.id===n.id?{...x,read_at:new Date().toISOString()}:x))};
 const markAll=async()=>{await api.adminReadAllNotifications();setNotifications(items=>items.map(x=>({...x,read_at:x.read_at||new Date().toISOString()})))};
 return <div style={{position:'fixed',left:76,top:82,width:'min(390px,calc(100vw - 95px))',maxHeight:'78vh',overflow:'auto',zIndex:90,background:'#fff',border:'1px solid #dfe4dd',borderRadius:16,boxShadow:'0 22px 70px rgba(16,24,40,.22)',padding:14}}>
  <div style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:10}}><div><strong>Notifications</strong><div style={{fontSize:12,color:'#667085'}}>Alerts + filters</div></div><button className="btn btn-light btn-small" onClick={onClose}>×</button></div>
  {error&&<div className="error-box" style={{marginTop:10}}>{error}</div>}
  <details style={{marginTop:12,border:'1px solid #edf0eb',borderRadius:12,padding:10}}><summary style={{cursor:'pointer',fontWeight:700}}>Alert filters</summary><div style={{display:'grid',gap:8,marginTop:10}}>{Object.entries(labels).map(([key,label])=><label key={key} style={{display:'flex',alignItems:'center',gap:8,fontSize:13}}><input type="checkbox" checked={draft[key]!==false} onChange={e=>setDraft({...draft,[key]:e.target.checked})}/>{label}</label>)}</div><button className="btn btn-dark btn-small" style={{marginTop:10}} disabled={busy} onClick={save}>Save filters</button></details>
  <div style={{display:'flex',justifyContent:'space-between',alignItems:'center',margin:'14px 2px 8px'}}><strong style={{fontSize:13}}>Recent</strong><button onClick={markAll} style={{border:0,background:'transparent',cursor:'pointer',fontSize:12}}>Mark all read</button></div>
  <div style={{display:'grid',gap:8}}>{notifications.slice(0,30).map(n=><button key={n.id} onClick={()=>mark(n)} style={{textAlign:'left',border:'1px solid #e7ebe5',borderLeft:n.read_at?'4px solid #d9ddd7':'4px solid #a7d83e',background:n.read_at?'#fff':'#f8fde9',padding:10,borderRadius:10,cursor:'pointer'}}><div style={{display:'flex',justifyContent:'space-between',gap:8}}><strong style={{fontSize:13}}>{n.title}</strong><small>{when(n.created_at)}</small></div><p style={{margin:'5px 0 0',fontSize:12}}>{n.message}</p></button>)}{!notifications.length&&<div className="empty-panel">No notifications yet.</div>}</div>
 </div>;
}

function Metric({label,value,accent}){return <div className={`ops-metric ${accent||''}`}><span>{label}</span><strong>{value}</strong></div>}
function CaseRow({item,selected,onClick}){return <button className={`ops-case-row ${selected?'selected':''}`} onClick={onClick}><div className="ops-case-main"><div><strong>{item.public_id}</strong><span className={`priority-dot priority-${item.priority}`}/></div><small>{item.name} · {item.phone}</small></div><div className="ops-case-service">{item.service?.title||title(item.service_slug)}<small>{item.source} · {item.locale?.toUpperCase()}</small></div><div><span className={`status-chip status-${item.status}`}>{item.status_label||title(item.status)}</span></div><div className="readiness-mini"><span style={{width:`${item.readiness_score||0}%`}}/><small>{item.readiness_score||0}%</small></div></button>}

function Detail({data,staff,user,onRefresh}){
 const isManager=manager(user);const [saving,setSaving]=useState(false),[note,setNote]=useState(''),[customerVisible,setCustomerVisible]=useState(false),[error,setError]=useState(''),[tab,setTab]=useState('overview');
 const [edit,setEdit]=useState({status:data.status,priority:data.priority,assigned_admin_user_id:data.assignee?.id||'',fee_rupees:((data.fee_paise||0)/100).toString()});
 useEffect(()=>setEdit({status:data.status,priority:data.priority,assigned_admin_user_id:data.assignee?.id||'',fee_rupees:((data.fee_paise||0)/100).toString()}),[data.public_id,data.status,data.priority,data.assignee?.id,data.fee_paise]);
 const save=async()=>{setSaving(true);setError('');try{const payload={status:edit.status,priority:edit.priority};if(isManager){payload.assigned_admin_user_id=edit.assigned_admin_user_id?Number(edit.assigned_admin_user_id):null;payload.fee_paise=Math.max(0,Math.round(Number(edit.fee_rupees||0)*100))}await api.adminUpdateCase(data.public_id,payload);await onRefresh()}catch(e){setError(e.message)}finally{setSaving(false)}};
 const addNote=async e=>{e.preventDefault();if(!note.trim())return;setSaving(true);try{await api.adminAddNote(data.public_id,note,customerVisible);setNote('');setCustomerVisible(false);await onRefresh()}catch(e){setError(e.message)}finally{setSaving(false)}};
 const verifyDoc=async(doc,status)=>{try{await api.adminVerifyDocument(doc.id,status);await onRefresh()}catch(e){setError(e.message)}};
 return <section className="ops-detail"><div className="ops-detail-head"><div><span className="section-eyebrow">CASE WORKSPACE</span><h2>{data.public_id}</h2><p>{data.name} · {data.phone} {data.email?`· ${data.email}`:''}</p></div><div className="ops-head-badges"><span className={`status-chip status-${data.status}`}>{data.status_label}</span><span className={`priority-badge priority-${data.priority}`}>{data.priority}</span></div></div><div className="ops-tabs">{['overview','documents','timeline'].map(name=><button key={name} className={tab===name?'active':''} onClick={()=>setTab(name)}>{title(name)}{name==='documents'&&<em>{data.documents?.length||0}</em>}</button>)}</div>{error&&<div className="error-box ops-error">{error}</div>}
 {tab==='overview'&&<div className="ops-detail-body"><div className="ops-two-col"><div className="ops-card"><div className="ops-card-head"><h3>Workflow control</h3><span>{isManager?'Manager controls':'Assigned case'}</span></div><div className="ops-form-grid"><label>Status<select value={edit.status} onChange={e=>setEdit({...edit,status:e.target.value})}>{STATUS_OPTIONS.map(v=><option key={v} value={v}>{title(v)}</option>)}</select></label><label>Priority<select value={edit.priority} onChange={e=>setEdit({...edit,priority:e.target.value})}>{PRIORITIES.map(v=><option key={v} value={v}>{title(v)}</option>)}</select></label>{isManager&&<><label>Assignee<select value={edit.assigned_admin_user_id} onChange={e=>setEdit({...edit,assigned_admin_user_id:e.target.value})}><option value="">Unassigned</option>{staff.filter(s=>s.active).map(s=><option key={s.id} value={s.id}>{s.name} · {s.role}</option>)}</select></label><label>Service fee (₹)<input type="number" min="0" value={edit.fee_rupees} onChange={e=>setEdit({...edit,fee_rupees:e.target.value})}/></label></>}</div><button className="btn btn-dark" disabled={saving} onClick={save}>{saving?'Saving…':'Save workflow changes'}</button></div><div className="ops-card case-health-card"><div className="ops-card-head"><h3>Case health</h3><span>{data.service?.title}</span></div><div className="health-ring" style={{'--score':`${data.readiness_score||0}%`}}><strong>{data.readiness_score||0}%</strong><span>readiness</span></div><div className="health-meta"><span><small>Payment</small><strong>{title(data.payment_status)}</strong></span><span><small>Fee</small><strong>{money(data.fee_paise)}</strong></span><span><small>Source</small><strong>{title(data.source)}</strong></span></div></div></div><div className="ops-card"><div className="ops-card-head"><h3>AI intake report</h3><span>{data.ai_report?'Ready':'Pending'}</span></div>{data.ai_report?<div className="ai-report-grid"><div className="ai-summary"><small>SUMMARY</small><p>{data.ai_report.summary}</p></div><div><small>NEXT STEP</small><p>{data.ai_report.recommended_next_step}</p></div><div><small>WARNINGS</small><p>{data.ai_report.warnings?.join(' · ')||'No AI warnings.'}</p></div></div>:<div className="empty-panel">AI report will appear after processing.</div>}</div><div className="ops-two-col"><div className="ops-card"><div className="ops-card-head"><h3>Structured intake</h3><span>{Object.keys(data.answers||{}).length} fields</span></div><dl className="answer-list">{Object.entries(data.answers||{}).map(([k,v])=><div key={k}><dt>{title(k)}</dt><dd>{v||'—'}</dd></div>)}</dl></div><div className="ops-card"><div className="ops-card-head"><h3>Case notes</h3><span>{data.notes?.length||0}</span></div><form className="note-form" onSubmit={addNote}><textarea rows="4" value={note} onChange={e=>setNote(e.target.value)} placeholder="Add internal context or customer update…"/><label className="switch-line"><input type="checkbox" checked={customerVisible} onChange={e=>setCustomerVisible(e.target.checked)}/> Visible to customer</label><button className="btn btn-dark btn-small" disabled={saving}>Add note</button></form></div></div></div>}
 {tab==='documents'&&<div className="ops-detail-body"><div className="ops-card"><div className="ops-card-head"><h3>Private document vault</h3><span>Authenticated access only</span></div><div className="document-grid">{(data.documents||[]).map(doc=><article className="document-card" key={doc.id}><div className="document-icon">{doc.mime_type?.includes('pdf')?'PDF':'FILE'}</div><div className="document-info"><strong>{doc.name}</strong><small>{doc.category} · {(doc.size_bytes/1024).toFixed(0)} KB</small><span className={`doc-status doc-${doc.status}`}>{title(doc.status)}</span></div><div className="document-actions"><button onClick={()=>api.adminViewDocument(doc.id,doc.name)}>View</button><button onClick={()=>api.adminDownloadDocument(doc.id,doc.name)}>Download</button><button onClick={()=>verifyDoc(doc,'verified')}>Verify</button><button onClick={()=>verifyDoc(doc,'rejected')}>Reject</button></div></article>)}{!data.documents?.length&&<div className="empty-panel">No documents uploaded.</div>}</div></div></div>}
 {tab==='timeline'&&<div className="ops-detail-body"><div className="ops-card"><div className="ops-card-head"><h3>Audit timeline</h3><span>Operational history</span></div><div className="timeline">{(data.events||[]).map(event=><article key={event.id}><span className="timeline-dot"/><div><strong>{title(event.type)}</strong><p>{event.message}</p><small>{event.actor_type} · {when(event.created_at)}</small></div></article>)}</div></div></div>}</section>;
}

function StaffView({user,staff,reload}){
 const [busy,setBusy]=useState(false),[error,setError]=useState(''),[message,setMessage]=useState(''),[form,setForm]=useState({name:'',email:'',phone:'',job_title:'',role:'agent',password:''});
 const create=async e=>{e.preventDefault();setBusy(true);setError('');try{await api.adminCreateStaff(form);setForm({name:'',email:'',phone:'',job_title:'',role:'agent',password:''});await reload();setMessage('Staff user created.')}catch(err){setError(err.message)}finally{setBusy(false)}};
 const patch=async(id,payload)=>{setBusy(true);try{await api.adminUpdateStaff(id,payload);await reload()}catch(err){setError(err.message)}finally{setBusy(false)}};
 return <div style={{display:'grid',gap:18}}><div><span className="section-eyebrow">TEAM & ACCESS</span><h1>Staff management</h1></div>{error&&<div className="error-box">{error}</div>}{message&&<div style={{...panel,borderColor:'#b7dd74'}}>{message}</div>}<form onSubmit={create} style={panel}><h3 style={{marginTop:0}}>Create staff user</h3><div style={grid}><label>Name<input style={input} required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Email<input style={input} type="email" required value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label>Phone<input style={input} value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Job title<input style={input} value={form.job_title} onChange={e=>setForm({...form,job_title:e.target.value})}/></label><label>Role<select style={input} value={form.role} onChange={e=>setForm({...form,role:e.target.value})}>{ROLES.map(r=><option key={r}>{r}</option>)}</select></label><label>Temporary password<input style={input} type="password" minLength="10" required value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label></div><button className="btn btn-dark" disabled={busy} style={{marginTop:15}}>Create user</button></form><div style={{display:'grid',gap:12}}>{staff.map(s=><div key={s.id} style={{...panel,display:'grid',gridTemplateColumns:'minmax(180px,1.4fr) minmax(130px,.7fr) auto',gap:12,alignItems:'center'}}><div><strong>{s.name}</strong><div style={{fontSize:12,color:'#667085'}}>{s.email}<br/>{s.job_title||'No job title'}</div></div><select style={input} disabled={s.id===user.id} value={s.role} onChange={e=>patch(s.id,{role:e.target.value})}>{ROLES.map(r=><option key={r}>{r}</option>)}</select>{s.id!==user.id?<button className="btn btn-light btn-small" onClick={()=>patch(s.id,{active:!s.active})}>{s.active?'Deactivate':'Activate'}</button>:<span>Current user</span>}</div>)}</div></div>;
}

function ProfileView({user,onUser}){
 const [form,setForm]=useState({name:user.name||'',phone:user.phone||'',job_title:user.job_title||'',timezone:user.timezone||'Asia/Kolkata'}),[pass,setPass]=useState({current_password:'',password:'',password_confirmation:''}),[message,setMessage]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);
 const save=async e=>{e.preventDefault();setBusy(true);setError('');try{const r=await api.adminUpdateProfile(form);onUser({...user,...r.user});setMessage('Profile updated.')}catch(err){setError(err.message)}finally{setBusy(false)}};
 const change=async e=>{e.preventDefault();setBusy(true);setError('');try{await api.adminChangePassword(pass);setPass({current_password:'',password:'',password_confirmation:''});setMessage('Password changed.')}catch(err){setError(err.message)}finally{setBusy(false)}};
 return <div style={{display:'grid',gap:18}}><div><span className="section-eyebrow">MY ACCOUNT</span><h1>Profile & security</h1></div>{error&&<div className="error-box">{error}</div>}{message&&<div style={{...panel,borderColor:'#b7dd74'}}>{message}</div>}<form onSubmit={save} style={panel}><h3>Profile</h3><div style={grid}><label>Name<input style={input} required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>Phone<input style={input} value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></label><label>Job title<input style={input} value={form.job_title} onChange={e=>setForm({...form,job_title:e.target.value})}/></label><label>Timezone<select style={input} value={form.timezone} onChange={e=>setForm({...form,timezone:e.target.value})}><option value="Asia/Kolkata">Asia/Kolkata</option><option value="UTC">UTC</option></select></label></div><button className="btn btn-dark" disabled={busy} style={{marginTop:15}}>Save profile</button></form><form onSubmit={change} style={panel}><h3>Change password</h3><div style={grid}><label>Current<input style={input} type="password" required value={pass.current_password} onChange={e=>setPass({...pass,current_password:e.target.value})}/></label><label>New password<input style={input} type="password" minLength="10" required value={pass.password} onChange={e=>setPass({...pass,password:e.target.value})}/></label><label>Confirm<input style={input} type="password" minLength="10" required value={pass.password_confirmation} onChange={e=>setPass({...pass,password_confirmation:e.target.value})}/></label></div><button className="btn btn-dark" disabled={busy} style={{marginTop:15}}>Change password</button></form></div>;
}

export default function AdminPortal(){
 const [user,setUser]=useState(null),[cases,setCases]=useState([]),[metrics,setMetrics]=useState({}),[staff,setStaff]=useState([]),[notifications,setNotifications]=useState([]),[selectedId,setSelectedId]=useState(''),[detail,setDetail]=useState(null),[filters,setFilters]=useState({search:'',status:'',priority:''}),[loading,setLoading]=useState(true),[error,setError]=useState(''),[view,setView]=useState('cases'),[alertsOpen,setAlertsOpen]=useState(false);const initialNotifications=useRef(true);
 const hasToken=!!localStorage.getItem('bkw_admin_token');
 const loadCases=async(next=filters)=>{const r=await api.adminCases(next);setCases(r.data||[]);setMetrics(r.metrics||{});if(!selectedId&&r.data?.[0])setSelectedId(r.data[0].public_id);if(selectedId&&!r.data?.some(x=>x.public_id===selectedId)){setSelectedId(r.data?.[0]?.public_id||'');setDetail(null)}};
 const loadDetail=async(id=selectedId)=>{if(!id)return;const r=await api.adminCase(id);setDetail(r.data)};
 const loadStaff=async()=>{if(!manager(user))return setStaff([]);const r=await api.adminStaff();setStaff(r.data||[])};
 const loadNotifications=async(sound=false)=>{const r=await api.adminNotifications();const next=r.data||[];if(sound&&!initialNotifications.current){const prevUnread=new Set(notifications.filter(n=>!n.read_at).map(n=>n.id));if(next.some(n=>!n.read_at&&!prevUnread.has(n.id)))playNotificationSound()}initialNotifications.current=false;setNotifications(next);return r};
 const boot=async()=>{setLoading(true);setError('');try{const me=await api.adminMe();setUser(me.user);const [caseR,noteR]=await Promise.all([api.adminCases(filters),api.adminNotifications()]);setCases(caseR.data||[]);setMetrics(caseR.metrics||{});setNotifications(noteR.data||[]);if(caseR.data?.[0])setSelectedId(caseR.data[0].public_id);if(manager(me.user)){const sr=await api.adminStaff();setStaff(sr.data||[])}}catch(err){localStorage.removeItem('bkw_admin_token');setUser(null);setError(err.message)}finally{setLoading(false)}};
 useEffect(()=>{if(hasToken)boot();else setLoading(false)},[]);
 useEffect(()=>{if(user&&selectedId)loadDetail(selectedId).catch(e=>setError(e.message))},[selectedId,user]);
 useEffect(()=>{if(!user)return;const id=setInterval(()=>loadNotifications(true).catch(()=>{}),20000);return()=>clearInterval(id)},[user,notifications]);
 const unread=notifications.filter(n=>!n.read_at).length;const selected=useMemo(()=>cases.find(c=>c.public_id===selectedId),[cases,selectedId]);
 const refresh=async()=>{await loadCases();if(selectedId)await loadDetail();if(manager(user))await loadStaff();await loadNotifications(false)};const logout=async()=>{try{await api.adminLogout()}catch{}localStorage.removeItem('bkw_admin_token');setUser(null)};
 if(!user)return <Login onLoggedIn={u=>{setUser(u);setLoading(false);setTimeout(()=>boot(),0)}}/>;if(loading)return <div className="admin-loading">Loading operations console…</div>;
 return <div className="ops-shell"><aside className="ops-sidebar"><div className="ops-brand"><span className="brand-mark">B</span><div><strong>BasKarwaDo</strong><small>Operations</small></div></div><nav><button onClick={()=>setAlertsOpen(v=>!v)} title="Notifications">🔔 <span>Alerts {unread>0?`(${unread})`:''}</span></button><button className={view==='cases'?'active':''} onClick={()=>setView('cases')}>◫ <span>{manager(user)?'Cases':'My cases'}</span></button>{manager(user)&&<button className={view==='staff'?'active':''} onClick={()=>setView('staff')}>♙ <span>Team</span></button>}<button className={view==='profile'?'active':''} onClick={()=>setView('profile')}>◎ <span>My profile</span></button><button onClick={()=>window.open('/','_blank')}>↗ <span>Customer site</span></button></nav><div className="ops-user"><div className="ops-avatar">{user.name?.[0]?.toUpperCase()}</div><div><strong>{user.name}</strong><small>{user.role}</small></div><button onClick={logout}>↪</button></div></aside><NotificationDropdown open={alertsOpen} onClose={()=>setAlertsOpen(false)} notifications={notifications} setNotifications={setNotifications} user={user} onUser={setUser}/><main className="ops-main">{error&&<div className="error-box ops-global-error">{error}</div>}{view==='cases'&&<><header className="ops-topbar"><div><span className="section-eyebrow">LIVE OPERATIONS</span><h1>{manager(user)?'Case command centre':'My assigned cases'}</h1></div><div className="ops-top-actions"><span className="live-indicator"><i/> Queue connected</span><button className="btn btn-light btn-small" onClick={refresh}>↻ Refresh</button></div></header><section className="ops-metrics"><Metric label="Open" value={metrics.open||0}/><Metric label="Ready" value={metrics.ready_for_review||0} accent="metric-mint"/><Metric label="Waiting customer" value={metrics.waiting_customer||0}/><Metric label="Urgent" value={metrics.urgent||0} accent="metric-red"/></section><section className="ops-workspace"><div className="ops-queue"><form className="ops-filters" onSubmit={async e=>{e.preventDefault();await loadCases(filters)}}><input value={filters.search} onChange={e=>setFilters({...filters,search:e.target.value})} placeholder="Search…"/><select value={filters.status} onChange={e=>setFilters({...filters,status:e.target.value})}><option value="">All statuses</option>{STATUS_OPTIONS.map(v=><option key={v} value={v}>{title(v)}</option>)}</select><select value={filters.priority} onChange={e=>setFilters({...filters,priority:e.target.value})}><option value="">All priorities</option>{PRIORITIES.map(v=><option key={v} value={v}>{title(v)}</option>)}</select><button className="btn btn-dark btn-small">Filter</button></form><div className="ops-queue-head"><span>{cases.length} loaded</span></div><div className="ops-case-list">{cases.map(item=><CaseRow key={item.public_id} item={item} selected={item.public_id===selectedId} onClick={()=>setSelectedId(item.public_id)}/>)}{!cases.length&&<div className="empty-panel">No assigned cases match this filter.</div>}</div></div><div className="ops-detail-wrap">{detail&&selected?<Detail key={detail.public_id} data={detail} staff={staff} user={user} onRefresh={refresh}/>:<div className="empty-detail"><strong>Select a case</strong><p>Case details will appear here.</p></div>}</div></section></>}{view==='staff'&&manager(user)&&<StaffView user={user} staff={staff} reload={loadStaff}/>} {view==='profile'&&<ProfileView user={user} onUser={setUser}/>}</main></div>;
}
